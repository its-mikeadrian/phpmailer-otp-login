<?php
// Minimal auth stub for future expansion.
// Currently used as a placeholder by register.php.

// You can add real auth helpers here later, for example:
// function auth_is_logged_in(): bool { return !empty($_SESSION['auth_user_id']); }